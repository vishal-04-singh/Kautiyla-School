import React from 'react';

const Button = () => {
  const handleGetStarted = () => {
    // Your handle function here
  };

  return (
    <button className='btn-n-n' onClick={handleGetStarted}>
      Get Started
    </button>
  );
};

export default Button;